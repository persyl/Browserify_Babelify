{
    "name": "browserifytesting",
    "version": "1.0.0",
    "description": "Just a testrepo",
    "main": "index.js",
    "scripts": {
        "start": "browserify index.js -t babelify --outfile bundle.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Per Lundkvist",
    "license": "ISC",
    "devDependencies": {
        "babel": "^6.5.2",
        "babel-preset-es2015": "^6.6.0",
        "babelify": "^7.2.0"
    },
    "browserify": {
        "transform": [
            ["babelify", { "presets": ["es2015"] }]
        ]
    }
}
